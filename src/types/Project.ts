export interface Project {
    id: number,
    title: string,
    description: string;
    cover: string;
    video: string;
    "live-link": string;
    tags: string [];
    group: string;
}